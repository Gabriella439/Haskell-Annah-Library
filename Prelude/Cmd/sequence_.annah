let sequence_ (m : * -> *) (cmds : #List (#Cmd m #Prod0 )) : #Cmd m #Prod0 =
    cmds
    (#Cmd m #Prod0 )
    (\(cmd1 : #Cmd m #Prod0 ) -> \(cmd2 : #Cmd m #Prod0 ) ->
        #Cmd/(>>=) #Prod0 #Prod0 m cmd1 (\(_ : #Prod0 ) -> cmd2)
    )
    (#Cmd/Pure m #Prod0 #Prod0/Make )
in  sequence_
