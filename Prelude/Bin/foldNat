λ(bin : #Bin ) → λ(Nat : *) → λ(Succ : Nat → Nat) → λ(Zero : Nat) → bin Nat Zero (λ(x : ∀(Bin_ : *) → ∀(Nil_ : Bin_) → ∀(Zero_ : Bin_ → Bin_) → ∀(One_ : Bin_ → Bin_) → Bin_) → x (∀(S : *) → ∀(MakeS : (Nat → Nat) → Nat → S) → S) (λ(S : *) → λ(MakeS : (Nat → Nat) → Nat → S) → MakeS Succ Zero) (λ(s : ∀(S : *) → ∀(MakeS : (Nat → Nat) → Nat → S) → S) → s (∀(S : *) → ∀(MakeS : (Nat → Nat) → Nat → S) → S) (λ(place : Nat → Nat) → λ(_ : Nat) → λ(S : *) → λ(MakeS : (Nat → Nat) → Nat → S) → MakeS (λ(n : Nat) → place (place n)) _)) (λ(s : ∀(S : *) → ∀(MakeS : (Nat → Nat) → Nat → S) → S) → s (∀(S : *) → ∀(MakeS : (Nat → Nat) → Nat → S) → S) (λ(place : Nat → Nat) → λ(total : Nat) → λ(S : *) → λ(MakeS : (Nat → Nat) → Nat → S) → MakeS (λ(n : Nat) → place (place n)) (place total))) Nat (λ(place : Nat → Nat) → place))
